{
  "name": "application",
  "version": "1.0.0",
  "description": "Your application.",
  "private": true,
  "engines": {
    "node": ">= 8.0.0 < 9.0.0"
  },
  "scripts": {
    "start": "if [ \"$NODE_ENV\" = \"production\" ]; then jemini start; else nodemon; fi"
  },
  "dependencies": {
    "@jemini/bookshelf": "^1.0.0",
    "jemini": "^1.0.0",
    "pg": "^7.5.0"
  },
  "devDependencies": {
    "nodemon": "^1.18.4",
    "sqlite3": "^4.0.2"
  }
}
